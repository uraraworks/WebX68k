{
  "name": "webx68k",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-2.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "test": "vitest run",
    "test:core": "vitest run test/core-serial-integration.test.ts",
    "capture-help": "node scripts/capture-help-shots.mjs",
    "verify:version": "node tools/verify_version_determinism.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^25.4.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
